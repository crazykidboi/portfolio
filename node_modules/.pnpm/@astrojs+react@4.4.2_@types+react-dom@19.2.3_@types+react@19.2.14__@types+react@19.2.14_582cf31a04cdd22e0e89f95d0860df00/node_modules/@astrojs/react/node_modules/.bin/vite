#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vite/bin/vite.js" "$@"
else
  exec node  "$basedir/../../../../vite/bin/vite.js" "$@"
fi
# cmd-shim-target=/Users/maxis/Documents/portfolio/node_modules/.pnpm/@astrojs+react@4.4.2_@types+react-dom@19.2.3_@types+react@19.2.14__@types+react@19.2.14_582cf31a04cdd22e0e89f95d0860df00/node_modules/vite/bin/vite.js
